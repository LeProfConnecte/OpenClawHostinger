# =============================================================================
# OpenClaw Backend - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   chmod 600 .env
# =============================================================================

# --- MongoDB ---
# Option 1: Local MongoDB (default for Hostinger VPS)
MONGO_URL=mongodb://127.0.0.1:27017
# Option 2: MongoDB Atlas (replace with your Atlas connection string)
# MONGO_URL=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database name
DB_NAME=openclaw_app

# --- CORS ---
# Comma-separated list of allowed origins (your domain with HTTPS)
# NEVER use * in production
CORS_ORIGINS=https://myopenclaw.leprofconnecte.com

# --- Cookie Security ---
# true for production (HTTPS required), false for local dev without HTTPS
COOKIE_SECURE=true
# lax for same-origin deployments (recommended), none for cross-origin dev setups
COOKIE_SAMESITE=lax

# --- Emergent Provider (default, no user API key needed) ---
# Get your key from Emergent Agent
EMERGENT_API_KEY=sk-emergent-your-key-here
EMERGENT_BASE_URL=https://integrations.emergentagent.com/llm
